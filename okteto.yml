name: hello-world
image: golang:buster
workdir: /app
command: ["bash"]
securityContext:
  capabilities:
    add:
    - SYS_PTRACE 
forward:
  - 8080:8080
  - 2345:2345
persistentVolume:
  enabled: false
